# Decod.es Python AST Serialization Module

This module uses python's ```ast``` module to parse python source and serialize the resulting AST as JSON.

This module requires ```pip``` and ```virtualenv``` as dependencies.